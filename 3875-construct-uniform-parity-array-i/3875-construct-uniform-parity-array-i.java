class Solution {
    public boolean uniformArray(int[] nums1) {

/*  if all elements in nums1 is even or odd then nums2 
    will have all even or odd elements so return true
      
    if all elements in nums1 are either odd or even than
    if nums2[i] = nums1[i] (even or odd)
    than for any other i
    nums2[i] = nums1[i] - nums[2]; will always odd or even 
    ie. even − even = even
        odd − odd = even
        even − odd = odd
        odd − even = odd
      */ 
    return true;
    }
}